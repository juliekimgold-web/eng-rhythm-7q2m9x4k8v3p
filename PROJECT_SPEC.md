# -ENG Flutter 구현 명세

## 제품 한 문장

일상의 비언어적 소리에서 리듬을 추출하고, 닮은 영어 단어의 음절·강세 패턴으로 변환해 청각·시각·촉각으로 익히는 학습 서비스.

## 핵심 경험

1. 디바이스가 주변 소리의 반복 패턴을 감지한다.
2. 앱이 소리의 진폭 파형이 아니라 짧고 긴 소리의 지속 시간 패턴을 먼저 보여준다.
3. 수집된 길이 패턴을 영어 음절 길이와 비교하고 가장 닮은 단어로 변환한다.
4. 단어 발음 속도에 맞춰 현재 재생되는 음절을 하이라이트하고 디바이스 진동과 동기화한다.
5. 저장된 단어를 실제 문장 속에서 다시 발견하면 발음과 진동으로 재활성화한다.

## 정보 구조

- 홈
  - 디바이스 연결 상태
  - 주간 수집 현황
  - 최근 수집 단어
  - 오늘의 리듬 팁
- 라이브러리
  - 갤러리/수집 기록 전환
  - 월별 캘린더와 날짜별 수집 단어 조회
  - 전체/즐겨찾기/박자별 필터
  - 단어 검색
  - 단어별 발음, 뜻, 소리 출처, 리듬 상세
- 수집
  - 소리의 지속 시간 감지
  - 짧고 긴 길이 패턴 시각화
  - 영어 음절 길이 비교
  - 유사 단어 변환 과정 표시
  - 라이브러리 저장
- 스캔
  - 카메라 또는 미디어 불러오기
  - OCR 텍스트 변환
  - 수집 단어 하이라이트
  - 발음·진동 동시 재생
- 디바이스
  - 연결/해제
  - 진동 강도
  - 잡음 필터링
  - 자동 재생
  - 배터리/펌웨어/동기화 정보

## 디자인 시스템

- Primary: `#FF7C11`
- Accent: `#FF9238`, 기능 CTA·선택 상태·길이 패턴에 제한적으로 사용
- Ink: `#282624`, `#6F6B67`
- Surface: `#F7F6F4`, 카드 `#FFFFFF`, 선 `#E4E0DC`, 보조 면 `#FFF3E8`
- Typography direction: Pretendard 계열, 절제된 굵기의 제목과 정보 중심 본문
- Shape: 작은 요소 8px, 컨트롤 10px, 카드 14px의 낮은 필렛. 원형은 연결 상태나 실제 디바이스 형상에만 사용
- Layout: 넉넉한 여백, 얇은 경계선, 최소한의 그림자로 카드 계층을 표현
- Motion: 길이 감지→패턴 확정→음절 매칭→결과 전환, TTS·진동과 동기화된 음절 강조

## 현재 프로토타입과 실제 연동의 경계

현재 코드는 UX 검증용 로컬 상태 기반 프로토타입이다. 실제 제품화 단계에서는 아래 어댑터가 필요하다.

- Bluetooth LE: 스캔, 페어링, 배터리, 진동 명령
- 오디오: 마이크 권한, 녹음, 잡음 제거, 리듬 특징 추출
- 변환 모델/API: 리듬 특징과 영어 단어 매칭
- OCR: 카메라/갤러리 입력과 텍스트 인식
- 음성: 영어 TTS와 발음 녹음
- 저장소: 사용자, 수집 기록, 즐겨찾기, 학습 연속일

비동기 장치·API 로직은 화면 상태와 분리해 서비스 계층으로 교체하는 것을 권장한다.
